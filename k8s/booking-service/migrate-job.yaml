apiVersion: batch/v1
kind: Job
metadata:
  name: booking-service-migrate
spec:
  backoffLimit: 2
  ttlSecondsAfterFinished: 600
  template:
    spec:
      restartPolicy: Never
      containers:
        - name: migrate
          image: booking-service:local
          command: ["node", "dist/db/migrate.js"]
          envFrom:
            - secretRef: { name: booking-service-secrets }