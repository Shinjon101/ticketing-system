apiVersion: batch/v1
kind: Job
metadata:
  name: booking-service-migrate
spec:
  backoffLimit: 2
  ttlSecondsAfterFinished: 600
  template:
    spec:
      restartPolicy: Never
      containers:
        - name: migrate
          image: ghcr.io/shinjon101/ticketing-booking-service:latest
          imagePullPolicy: Always
          command: ["node", "dist/db/migrate.js"]
          envFrom:
            - secretRef: { name: booking-service-secrets }